package com.gruppe78;

public class eventHandler{
    /*private IOState IO = new IOState(numFloor);
    public void main(){
        while(1){
        IOState newIO = new IOState(numFloor;
        try{Thread.sleep(50);}
        catch (Exception e){System.out.println(e);}
        newIO.elevFloor = DriverHandler.getElevatorFloor();
        for (int i = 0; i < numFloor; i++){
            newIO.buttonUp[i] = DriverHandler.isButtonPressed(0, i);
            newIO.buttonDown[i] = DriverHandler.isButtonPressed(1, i);
            newIO.buttonInternal[i] = DriverHandler.isButtonPressed(2, i);
        }
        newIO.stopButton = DriverHandler.isStopPressed();
        //Check for change, call state machine 
        if ((IO.elevFloor || newIO.elevFloor) && newIO.elevFloor != -1)|| newIO.buttonDown != IO.buttonDown 
        || newIO.buttonUp != IO.buttonUp || newIO.buttonInternal != IO.buttonInternal)){
            //call state machine.
        }
        IO = newIO;
        }
    }*/
}